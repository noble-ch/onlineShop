export declare type ViewSizeParams = {
    width: number;
    height: number;
    scrollHeight: number;
    scrollWidth: number;
};
export declare class View {
    scrollContainer: HTMLElement | undefined;
    width: number;
    height: number;
    scrollHeight: number;
    scrollWidth: number;
    constructor(config: {
        width: number;
        height: number;
        scrollHeight: number;
        scrollWidth: number;
        scrollContainer?: HTMLElement;
    });
    hasChanged(params: ViewSizeParams): boolean;
    setSize(params: ViewSizeParams): this;
}
